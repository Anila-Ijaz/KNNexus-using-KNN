# KNNexus-using-KNN
This project implements a K-Nearest Neighbors (KNN) classifier from scratch and compares its performance with scikit-learn models on multiple datasets.  The evaluation includes accuracy, runtime analysis, and confusion matrices to assess model performance.
